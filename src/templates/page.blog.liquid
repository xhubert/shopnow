<div class="page-blog--tpl container">
  <div class="inner">
    {% include 'breadcrumbs' %}
    {% include 'breadcrumbs' %}
    <h1 class="section-title">{{ page.title }}</h1>

    <div class="page-content__wrapper page-blog">
      <div class="page-content rte">
        {{ page.content }}
      </div>

      {% section 'blog-posts' %}
    </div>
  </div>

</div>
