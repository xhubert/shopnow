<div class="page--tpl container">
  <div class="inner">
    {% include 'breadcrumbs' %}
    <h1 class="section-title">{{ page.title }}</h1>
    <div class="page-content__wrapper">
      <div class="page-content rte" style="margin-bottom: 16px;">
        {{ page.content }}
      </div>
    </div>
  </div>
</div>
